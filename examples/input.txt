$sql = <<<sql
SELECT * FROM fridge
	WHERE
		`action` = $action,
		AND ip = '{$ip}'
		AND test1 = {$_GET['TEST']}
		AND test2 = {$_GET['TEST']['test2']}

	LIMIT $limit
sql;


$loginAttempts = $this->Core->fromDatabase($sql, "@raw", true);